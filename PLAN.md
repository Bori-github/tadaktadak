# 타닥타닥 작업 계획

## 작업 계획

시안 `design/prototype.html`의 코드를 재료로 쓴다.

### 초기 세팅

- [x] 프로젝트 생성
- [x] 패키지 설치 (Expo SDK 버전에 맞춤)
  - `expo-notifications` 로컬 알림이 Expo Go에서 동작하는지 첫날 확인한다
- [x] 팔레트와 수치 상수 파일. 상수는 `DESIGN.md` §7의 확정값 4개만 두고 나머지는 계산한다
- [x] 빈 Widget Extension 타겟으로 prebuild·서명·설치 확인. App Group은 무료 Personal Team으로 켤 수 없어 뺀다

### 기능·화면 구현 (Expo Go)

- [x] 반응형 계산과 시계판 좌표
- [x] 정적 화면 배치
  - [x] 시계판 (눈금 숫자, 장작·모닥불, 호·손잡이·기준 표식)
  - [x] 집중·휴식 타이머 숫자
  - [x] 조작 버튼
- [x] 손잡이 조작(각도는 worklet에서 계산), 숫자 탭으로 편집 대상 전환
- [x] 시간 모델. 아래 순서로 만든다.
  - [x] 남은 시간 계산과 점화 판정 (순수 함수)
  - [x] 단계 전이, 앱 재실행 복구, 저장값 읽기 (순수 함수)
  - [x] 화면 배선. 버튼 조작, 카운트다운 숫자, 진행 중 손잡이 끌기 차단, 화면 꺼짐 방지, 포그라운드 복귀 재보정
  - [x] 저장값 쓰기와 앱 재실행 연결
  - [x] 호와 손잡이를 UI 스레드로. 각도는 남은 시간 공유 값에서 매 프레임 파생한다
    - 설정 범위는 집중 1분에서 60분, 표시 범위는 0에서 설정 시간이다. `TIMER_RANGE`로 클램프하면 호가 1분에서 멈춘다
    - 남은 분 공유 값을 매 프레임 쓴다. 값을 끊어 쓰면 호가 계단이 되므로, 실기기에서 UI 프레임이 떨어지는 것을 확인한 뒤에 손댄다
  - [x] 진행 중 손잡이 색은 `DESIGN.md` §8대로 크림. `useDialDrag`의 제스처를 `useMemo`로 감싼다
    - `useFrameCallback`에 인라인 워클릿을 넘겨 렌더마다 재등록된다. `useCallback`으로 감싸면 React Compiler 린트가 공유 값 쓰기를 막아, 첫 프레임 지연 초기화를 없애야 한다
    - `useMemo`의 의존성에 설정 분이 있어 끌 때는 스냅마다 제스처를 다시 만든다. 없애려면 `useDialDrag`가 분을 공유 값으로 받아야 한다
  - [x] 집중 타이머 개체 점화. 타는 프레임 격자를 시안에서 옮기고 `DialItems`를 Skia `Atlas` 하나로 바꾼다
    - 불꽃 A·B 교차는 인터벌을 쓰는 훅이라 단위 테스트로 잡지 않는다. 배속으로 돌려 화면으로 확인한다
  - [x] 휴식 타이머 점화. `DESIGN.md` §8대로 불이 꺼지고 호가 차오른다
  - [x] 일시정지 불 밝기 35%. `DESIGN.md` §8 §9
    - 시간 애니메이션이라 단위 테스트로 잡지 않는다. 배속으로 돌려 일시정지했을 때 500밀리초에 걸쳐 잦아드는지 화면으로 확인한다
  - [x] 설정 밖 눈금 어둡게. `DESIGN.md` §5
    - 꺼진 눈금을 `Atlas` 둘로 나눠 그리고 설정 밖만 알파 0.55로 덮는다. 좌표는 다시 만들지 않고 자르기만 해서 끌 때 60칸 재계산이 늘지 않는다
    - 설정 밖 눈금은 Jest에 `CanvasKit`이 없어 `Skia.Surface.Make`가 예외를 던진다. 25분으로 두고 기기 밝기를 낮춰 화면으로 확인한다
  - [x] 휴식 타이머 자동 시작. 완료 → 휴식 → 집중 대기
  - 알림과 Live Activity는 각자의 단계에서 상태 전이에 더한다
- [x] 개발 빌드 배속. 타이머 시간을 5~50배로 돌린다. 조작은 `__DEV__`에서만 보이고 대기에서만 바뀐다
  - 배속은 저장하지 않는다. 리로드나 재실행 뒤에는 1배로 돌아가고 화면이 실제 남은 시간을 보여 준다. 끝날 시각은 그대로다
- [x] 빛 번짐. 반지름별 텍스처를 앱 시작 때 한 번 만들고 알파·스케일만 바꿔 그린다
- [x] 불티, 스파크 반짝임, 완료 연출
- [ ] 진동
  - [x] iOS 완료 진동. Core Haptics를 여는 로컬 모듈을 직접 만든다
  - [ ] Android 완료 진동
  - [x] 손잡이 스냅과 버튼 누름의 약한 진동
- [x] 알림 (예약, 취소, 권한)
  - 문구는 「집중 끝!」과 「휴식 끝!」이다. 권한이 없으면 문구 대신 알림 설정 버튼을 띄운다
- [x] 화면 회전 대응 — 세로 전용으로 확정. iPad는 `requireFullScreen`으로 가로·Split View를 열지 않는다
- [ ] 검증
  - 단위 테스트는 Jest, E2E는 Maestro. 단위 테스트는 `now`와 저장값을 인자로 받는 순수 함수를 대상으로 한다
  - [x] 손잡이 각도 → 분 — 네 방향, 1분 스냅 경계, 양 끝 클램프 (단위)
  - [x] 터치한 곳이 손잡이를 잡는 범위 안에 있는지 — 11 도트 경계와 대각선 (단위)
    - 끌기 자체는 실기기로 확인한다. 빠르게 끌 때 12시에서 반대편 끝으로 튀는지, 숫자를 터치했을 때 끌기가 탭을 가로채는지
  - [x] 숫자 터치 영역 — 두 버튼이 숫자 자리를 덮고 서로 겹치지 않는가 (화면 조립)
    - 숫자 탭이 대기 상태에서만 열리는 것과 재생할 때 편집 대상이 집중 타이머로 돌아가는 것은 `TimerScreen`의 조건이라 단위로 잡지 않는다. 휴식 타이머 숫자를 탭하고 재생해 타이머 사이클을 돌린 뒤 집중 타이머가 활성인지 화면으로 확인한다
  - [x] 편집 대상별 손잡이 색 — 집중 크림, 휴식 청록 (화면 조립)
    - 호 색은 Jest에 `CanvasKit`이 없어 `Skia.PathBuilder`가 예외를 던진다. 화면으로 확인한다
  - [x] 손잡이 반짝임 프레임 — 반짝이는 동안 A와 B, 멈춘 동안 A (단위)
  - [x] 손잡이 반짝임 배선 — 125밀리초마다 A와 B를 번갈아 그리는가 (화면 조립)
  - [x] 손잡이를 잡는 범위 게이트 — 손잡이 밖을 잡으면 끌어도 시간이 바뀌지 않는가 (단위)
  - [x] 끌기 저장 시점 — 스냅마다 시간이 바뀌고, 저장은 손을 뗄 때 마지막 값으로 한 번 (단위)
    - `fireGestureHandler`는 BEGAN·ACTIVE·END만 내보내 `onTouchesDown`·`onTouchesMove`를 부르지 못한다. 제스처의 터치 콜백을 직접 부르고 공유 값을 테스트 파일에서 대신 넣는다
  - [x] 단계에 따른 조작 버튼 — 네 단계의 아이콘과 두 버튼 잠금 (단위)
  - [x] 숫자 한 줄이 보여 줄 초 — 카운트다운 중인 줄과 아닌 줄 (단위)
  - [x] 조작 버튼 자리 — 터치 영역 64×64, 사이 24 논리 픽셀, 단계별 잠김 (화면 조립)
  - [x] 남은 시간 계산 — 흐른 만큼 줄어드는가, 재개 자리에서 이어지는가, 0 아래로 내려가지 않는가 (단위)
    - 시스템 시각을 바꿔도 흔들리지 않는 것은 기기 가동 시간만 인자로 받는 설계로 지킨다. 테스트로 잡으려면 `Date.now`를 가로막아야 해서 쓰지 않는다
  - [x] 배속 환산 — 배속만큼 짧아진 설정 시간, 배속만큼 커진 표시 시간 (단위)
  - [x] 남은 시간을 분으로 — 1초 미만이 남는가, 0 아래로 내려가지 않는가, 배속만큼 커진 분 (단위)
  - [x] 일시정지 복구의 남은 분 — 저장값에서 되살린 자리에 호가 멈춰 서는가 (단위)
    - 호가 줄어드는 것 자체는 배속 50배로 돌려 화면으로 확인한다. `Skia.PathBuilder`가 Jest에서 예외를 던진다
  - [x] 점화 판정 — `DESIGN.md` §8 표의 세 분기와 경계, 설정 밖 칸 (단위)
  - [x] 아틀라스 배치 — 격자 사이 여백, 첫 줄이 짧은 격자의 폭, 빈 목록 (단위)
  - [x] 개체 규격 — `DESIGN.md` §4 표의 폭·높이, 7도트가 9도트에서 위 두 줄을 뺀 것인가, 격자에 색 없는 글자가 없는가 (단위)
    - 기준 표식이 불 뒤에 그려지는지와 도트 가장자리는 Jest가 보지 못한다. 화면으로 확인한다
  - [x] 단계 전이 — 재생·일시정지·재개·정지·완료 (단위)
  - [x] 앱 재실행 — `SPEC.md` 앱 재실행 표 4행, 끝날 시각이 지금과 같은 경계, 정지됨 플래그 (단위)
  - [x] 저장값 읽기 — 깨진 값, 빈 값, 단계에 안 맞는 값, 범위 밖 분 (단위)
  - [x] 저장값 쓰기 — 단계마다 담는 키와 대기에서 저장할 것이 없는 자리 (단위)
    - 기기 저장소 읽고 쓰기와 앱 재실행 배선은 아래 「진행 중 종료 → 재실행」이 잡는다. Maestro를 깔기 전까지는 실기기로 확인한다
  - [x] 복구한 단계의 남은 시간 — 네 단계와 이미 지난 끝날 시각 (단위)
  - [x] 저장값을 읽는 사이의 조작 — 읽기가 끝나기 전의 재생과 끌기, 조작이 없을 때의 저장값 복구 (단위)
  - [x] 완료한 시각 — 완료 전이가 담는 값, 저장값에서 읽기, 끝난 시각이 없는 저장값 (단위)
  - [ ] 상태 전이 시 처리 — 진행 진입·이탈에서 화면 꺼짐 방지 호출 (단위). 알림과 Live Activity는 각자의 단계에서 더한다
  - [x] 반응형 구간 경계 — 배율·모닥불·반지름 상한이 갈리는 폭 (단위)
  - [x] 겹침 검산 — 1분 간격과 개체 사이 여유 (단위)
  - [x] 화면 높이별 버튼·시계판 위치 (단위)
  - [x] 알림 설정 버튼 자리와 터치 영역 — 기준 화면 좌표, 기기별 가장자리 거리, 56 × 56 (단위)
  - [x] 알림을 걸 시각 — 진행 여부, 권한 상태 넷, 이미 지난 끝날 시각 (단위)
  - [x] 도트 원 — 줄·열 양 끝 테두리, 차지 범위, 하이라이트와 그림자가 만나는 자리 (단위)
  - [x] 완료에서 다음으로 — 끝난 시각부터 시작하는 휴식 타이머 진행, 휴식 타이머도 끝난 경계, 미래인 끝난 시각, 0분이면 현재 시각부터 시작하는 집중 타이머 진행 (단위)
  - [x] 완료 뒤 자동 시작 — 집중 타이머가 끝난 그 자리에서 휴식 타이머 진행, 휴식 타이머가 0분이면 끝난 시각에서 3500밀리초 뒤 집중 타이머 대기 상태, 저장값으로 읽은 완료의 연출 길이 경계, 휴식 타이머 완료는 기다리지 않고 집중 타이머 대기 상태 (단위)
  - [x] 완료 연출 남은 시간 — 집중 타이머 완료의 연출 길이 경계, 휴식 타이머 시작 뒤 연출 길이 경계, 미래인 끝난 시각, 연출이 없는 단계 넷 (단위)
  - [x] 완료 연출 배선 — 일시정지 뒤 재개, 연출 길이 경계, 연출이 없는 단계, 끝난 시각, 완료에서 휴식 타이머 진행으로 넘어갈 때 (단위)
  - [x] 불티 궤적 — 편차의 양 끝, 감쇠한 자리, 튀기 전과 꺼진 뒤의 남은 수명, `lifeMs`가 `elapsedMs`와 같아지는 경계 (단위)
    - 튀어 오르는 모습과 잔광은 Jest에 `CanvasKit`이 없어 `Skia.Surface.Make`가 예외를 던진다. 배속으로 돌려 화면으로 확인한다
    - 경과를 받은 자리에서 이어 그리는 것도 화면으로 확인한다. 백그라운드에서 집중을 끝내고 1초 뒤 돌아와, 불티가 모닥불이 아니라 중간 높이에서 나타나는지 본다
    - 불티가 사라지는 것은 `useCompletedEffectStartedAt`이 `null`을 줄 때뿐이다. 3500밀리초에 맞춰 꺼지는지 함께 본다
  - [x] 타이머 사이클 — 1분 집중 → 휴식 → 대기 (E2E)
  - [ ] 진행 중 종료 → 재실행 — 남은 시간이 이어지는가. 휴식 도중 종료도 (E2E)
  - [ ] 백그라운드 복귀 — 돌아온 뒤 남은 시간이 맞는가 (E2E)
  - [ ] 진행 중 손잡이 드래그 — 무시되는가 (E2E)
  - [ ] 권한 거부 — 타이머가 끝까지 돌고, 알림 설정 버튼을 누르면 시스템 설정이 열리는가 (E2E)
  - [ ] 터치 — 두 버튼과 숫자 탭 영역이 겹치지 않는가 (E2E)
  - [x] 완료 진동 — 타이머별 패턴, 저장값으로 읽은 완료, 정지와 겹칠 때 (단위)
    - Jest는 `@modules/haptic-pattern`을 모의해 넘긴 이벤트까지만 본다. 실제 동작은 실기기로 확인한다
  - [x] 완료 진동 두 패턴이 다르게 느껴지는가 (실기기)
  - [x] 진동 세기, 알림 도착 (실기기)
  - [x] 조작 진동 — 스냅 분기, 자동 종료를 막고 되돌리는 짝, 모듈이 없는 경로 (단위)
  - [x] 집중·휴식 양쪽에서 스냅 진동 (실기기)
  - [x] 손잡이를 잡은 채 홈으로 나갔다 돌아오기 — 자동 종료가 되돌아가는가 (실기기)
  - [x] 빠르게 한 바퀴 — 진동이 손가락을 따라오는가 (실기기)

### Live Activity (개발 빌드)

- [ ] 개발 빌드 전환 (서명). 120Hz 설정 (`app.json` `ios.infoPlist`의 `CADisableMinimumFrameDurationOnPhone`)
- [ ] Widget Extension 타겟 추가와 앱 연결 (`@bacons/apple-targets`)
  - 실시간 현황 스위치가 켜져 있는지 확인하는 방법을 정한다
- [ ] 정지됨 플래그를 앱에 전달하는 방식을 정한다. App Group은 Apple Developer Program 가입이 필요하다. 대안은 App Intent의 `openAppWhenRun`. 근거는 `.claude/docs/live-activity.md`
- [ ] ActivityKit 브리지 모듈 (Expo 로컬 모듈, Swift와 TypeScript). 시작·종료, 정지됨 플래그 읽기
- [ ] Live Activity 화면 (SwiftUI. 아이콘, 남은 시간, 진행 막대, 정지 버튼)
- [ ] 정지 버튼 App Intent (Swift). 알림 취소, Live Activity 종료, 정지됨 플래그 쓰기
- [ ] 상태 전달과 시작·일시정지·종료 연동
- [ ] 실기기 검증 (Live Activity 표시, 정지 버튼 → 앱 재실행 시 대기, 스와이프 해제)

## 개발 환경

### 현재 환경

| 항목    | 값                                       |
| ------- | ---------------------------------------- |
| 모델    | MacBookPro16,1 (2019년 16인치, Intel i9) |
| macOS   | 26.6.1 Tahoe                             |
| Xcode   | 26.6 (17F113), Universal                 |
| iOS SDK | 26.5                                     |
| Node.js | 22.23.2                                  |

- Xcode 26.6이 이 기계에서 돌아가는 마지막 정식 빌드다. Xcode 27부터 Universal 빌드를 내놓지 않는다

### Expo Go

- iPhone에 Expo Go를 설치하고 Mac에서 개발 서버를 켜면 같은 와이파이에서 QR 코드로 실행된다

Expo SDK 57 기준. `npx expo install`이 SDK에 맞는 버전을 고른다.

| 패키지                                      | 버전    | 역할                                   | Expo Go               |
| ------------------------------------------- | ------- | -------------------------------------- | --------------------- |
| `expo`                                      | 57.0.15 | SDK                                    | —                     |
| `@shopify/react-native-skia`                | 2.6.2   | 렌더링                                 | 포함됨                |
| `react-native-reanimated`                   | 4.5.1   | 애니메이션                             | 포함됨                |
| `react-native-gesture-handler`              | 2.32.0  | 제스처                                 | 포함됨                |
| `expo-keep-awake`                           | 57.0.1  | 화면 꺼짐 방지                         | 포함됨                |
| `@react-native-async-storage/async-storage` | 2.2.0   | 영속 저장                              | 포함됨                |
| `expo-notifications` 로컬 알림              | 57.0.13 | 종료 알림                              | 사용 가능             |
| Widget Extension (SwiftUI)                  | —       | Live Activity                          | **불가능. 개발 빌드** |
| ActivityKit 브리지 (Expo 로컬 모듈)         | —       | Live Activity 시작·종료, 정지됨 플래그 | **불가능. 개발 빌드** |

### 개발 빌드

- `@bacons/apple-targets`: `targets/` 폴더의 SwiftUI 코드를 prebuild 때 Widget Extension 타겟으로 붙여 준다. Xcode 16 이상을 요구한다
- Team ID는 `.env`의 `EXPO_APPLE_TEAM_ID`에 둔다. `app.config.js`가 읽어 두 타겟의 서명 팀을 채운다

| 서명                    | 비용      | 기기 직접 설치       | TestFlight | App Store |
| ----------------------- | --------- | -------------------- | ---------- | --------- |
| 무료 Personal Team      | 없음      | 가능. 7일마다 재설치 | 불가       | 불가      |
| Apple Developer Program | 연 99달러 | 가능. 1년            | 가능       | 가능      |

- 무료 Personal Team은 App Group과 Push Notifications를 켤 수 없다
- 로컬 알림만 쓰므로 `plugins/withoutPushEntitlement.js`가 `expo-notifications`의 `aps-environment`를 지운다. 원격 푸시가 필요해지면 이 파일을 지우고 Apple Developer Program에 가입한다

### Maestro

E2E는 시뮬레이터 개발 빌드를 대상으로 한다. Expo Go는 `appId`가 `host.exp.Exponent`라 앱을 특정하지 못한다.

| 항목    | 값                                       |
| ------- | ---------------------------------------- |
| Maestro | 2.10.0                                   |
| Java    | Temurin 21. Maestro가 17 이상을 요구한다 |

- 설치는 `curl -fsSL https://get.maestro.mobile.dev | bash`
- `pnpm ios`로 앱을 올리고 알림 권한 창을 한 번 허용한 뒤 `pnpm e2e`를 실행한다
- 플로우에 `clearState`를 사용하지 않는다. 알림 권한까지 지워져 매 실행 권한 창이 뜬다
- `testID`는 `<슬라이스>-<대상>` 형태로 작성한다. `controls-play`, `readout-focus`
- 시계판 드래그는 `GestureDetector`가 화면 전체를 덮어 `testID` 없이 좌표 스와이프로 짚는다

### Android

`pnpm android` 개발 빌드로 개발·검증한다.

| 항목           | 값            |
| -------------- | ------------- |
| Android Studio | 2026.1        |
| SDK Platform   | 36            |
| Build-Tools    | 36.0.0        |
| NDK            | 27.1.12297006 |
| Java           | Temurin 21    |

- 설치는 `brew install --cask android-studio android-commandlinetools`
- SDK 구성 요소는 `sdkmanager --sdk_root=$HOME/Library/Android/sdk "platform-tools" "platforms;android-36" "build-tools;36.0.0" "ndk;27.1.12297006" "emulator" "system-images;android-36;google_apis;x86_64"`
- AVD는 `avdmanager create avd -n Pixel_10 -k "system-images;android-36;google_apis;x86_64" -d pixel_10`
- `~/.zshrc`에 아래를 추가한다

```sh
export JAVA_HOME="$(/usr/libexec/java_home -v 21)"
export ANDROID_HOME="$HOME/Library/Android/sdk"
export PATH="$PATH:$ANDROID_HOME/emulator:$ANDROID_HOME/platform-tools"
```

**유의사항**

- Android용 Expo Go에서는 `expo-notifications` 55 이상이 import 시점에 예외를 던져 앱이 뜨지 않는다
