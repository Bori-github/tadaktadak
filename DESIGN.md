---
id: tadaktadak
name: "타닥타닥"
category: personal-tool
bundle_id: "com.boriguri.tadaktadak"
primary_color: "#ffb43c"
omd: "0.1"
tokens:
  source: hand-authored
  grid:
    unit: dot
    dot_size: 2          # 배율 1에서 논리 픽셀
    scale: [1, 2, 3]     # 정수 배율만
    antialias: false     # 개체와 숫자. 호와 빛 번짐은 예외
  colors:
    canvas: "#141021"
    log: "#8a6238"
    log-shade: "#4a3220"
    fire-core: "#fff2a8"
    fire-mid: "#ffb43c"
    fire-base: "#e04a2b"
    ignite-bright: "#ffd94a"
    ignite-mid: "#ff9d2e"
    bloom: "#ff9d2e"
    focus-arc: "#6a5b9c"
    focus-text: "#f4ead6"
    rest-arc: "#3f9aa6"
    rest-text: "#8fdbe4"
    tick: "#7a6ea8"
    marker: "#5c5180"
    stone-face: "#6b6280"
    stone-edge: "#2b2638"
  typography:
    family: "5×7 도트 비트맵. 직접 그린다"
    glyphs: "0~9와 콜론"
    timer:     { dots: 21, scale: 3, use: "시계판 중심" }
    rest:      { dots: 7,  scale: 1, use: "타이머 아래" }
    countdown: { dots: 7,  scale: 1, use: "0분 눈금 바깥" }
    tick:      { dots: 7,  scale: 1, use: "시간 눈금 숫자" }
  orbit:
    slots: 60
    base_width: 375       # 배율이 오르는 기준
    base_diameter: 306
    r_object: 153
    r_arc: 131
    r_tick: 175
    arc_width: 6
  components:
    log:     { dots: "3×4",   use: "1분 칸" }
    bonfire: { dots: "7×9",   use: "5분 배수 칸. 좁은 화면에서 7×7" }
    handle:  { dots: "7×7",   use: "스파크. 남은 시간 끝점" }
    marker:  { dots: "3×5",   use: "12시 기준점" }
    ember:   { dots: "1×1",   use: "불티" }
    button:  { dots: "28×28", icon: "12×12", use: "조작 버튼 둘" }
---

# 타닥타닥

모닥불 집중 타이머의 화면 명세. 수치에는 단위를 함께 쓴다. 논리 픽셀과 도트를 구분한다.

## 1. Visual Theme & Atmosphere

어두운 방에서 모닥불 하나를 바라보는 화면이다. 배경은 거의 검은 남보라 `#141021`이고, 그 어둠 위에 유일한 광원으로 불이 있다. 시계판 한 바퀴에 장작과 모닥불이 어둡게 깔려 있고, 타이머가 돌면 하나씩 불이 붙는다. 불붙은 칸 수가 곧 지나온 분이라, 시간이 갈수록 화면이 따뜻해지고 밝아진다. 숫자를 읽지 않아도 얼마나 지났는지 불빛의 양으로 안다.

시각 언어는 픽셀 아트다. 개체와 숫자는 도트로 딱딱하게 서 있고, 그 아래에서 호와 빛 번짐이 부드럽게 번진다. 딱딱함과 부드러움이 한 화면에 겹쳐 있는 것이 이 디자인의 인상이다. 불꽃과 불티와 스파크는 일시정지가 아닌 한 늘 움직여서, 정지 화면이 아니라 타고 있는 불로 보인다.

## 2. Color Palette & Roles

### 장작과 모닥불

| 역할 | 값 | 쓰는 곳 |
|---|---|---|
| 배경 | `#141021` | 화면 전체 |
| 장작 | `#8a6238` | 타지 않은 장작 몸통 |
| 장작 그늘 | `#4a3220` | 장작 아랫면 |
| 불 심 | `#fff2a8` | 불꽃 가장 안쪽, 갓 튄 불티 |
| 불 가운데 | `#ffb43c` | 불꽃 중간, 식어가는 불티 |
| 불 아래 | `#e04a2b` | 불꽃 밑동, 꺼지는 불티 |
| 장작 점화 밝은 쪽 | `#ffd94a` | 불붙은 장작 윗면 |
| 장작 점화 중간 | `#ff9d2e` | 불붙은 장작 몸통 |
| 빛 번짐 | `#ff9d2e` | 부드러운 레이어의 광원 |

### 집중

| 역할 | 값 |
|---|---|
| 호 | `#6a5b9c` |
| 집중 타이머 숫자 | `#f4ead6` |
| 손잡이 갈래 | `#c9bb98` |
| 손잡이 심 | `#fff8e0` |
| 눈금 숫자 | `#7a6ea8` |
| 기준 표식 | `#5c5180` |

### 휴식

| 역할 | 값 |
|---|---|
| 호 | `#3f9aa6` |
| 휴식 타이머 숫자 | `#8fdbe4` |
| 손잡이 갈래 | `#2f7b85` |
| 손잡이 심 | `#9fe3ea` |

### 조작 버튼

| 역할 | 값 |
|---|---|
| 테두리 | `#2b2638` |
| 면 | `#6b6280` |
| 하이라이트 | `#8f86a8` |
| 그림자 | `#4a4360` |
| 아이콘 | `#f4ead6` |
| 잠긴 면 | `#3f3550` |
| 잠긴 아이콘 | `#6f6688` |

## 3. Typography Rules

폰트 패밀리가 없다. **5×7 도트 비트맵 자형 한 벌을 직접 그린다.** 글자는 `0~9`와 콜론뿐이고, 크기 차이는 배율로 낸다.

| 숫자 | 배율 | 크기 | 색 |
|---|---|---|---|
| 집중 타이머. 진행 중이면 카운트다운 | 3 | 21 도트 | 집중 `#f4ead6` |
| 휴식 타이머. 진행 중이면 카운트다운 | 1 | 7 도트 | 휴식 `#8fdbe4` |
| 휴식 시작 전 카운트다운 (남은 초) | 1 | 7 도트 | 휴식 `#8fdbe4` |
| 시간 눈금 | 1 | 7 도트 | `#7a6ea8` |

| 타이머 상태 | 알파(불투명도) |
|---|---|
| 활성 (카운트다운 중이거나 편집 대상으로 선택한 상태) | 1 |
| 비활성 | 0.4 |

## 4. Component Stylings

| 개체 | 도트 | 최대 투영 | 프레임 |
|---|---|---|---|
| 장작 | 3 × 4 | 4 | 타지 않은 것 1, 타는 것 3 |
| 모닥불 | 7 × 9 | 9 | 붙지 않은 것 1, 타는 것 6 |
| 스파크 손잡이 | 7 × 7 | 7 | 2 (반짝임) |
| 기준 표식 | 3 × 5 | 5 | 1 |
| 불티 | 1 × 1 | 1 | 1 |
| 조작 버튼 | 28 × 28 | | 기본, 눌림 |

최대 투영은 이웃 개체와 겹치는지 판정할 때 쓰는 값이다.

- 개체가 회전하지 않으므로 둘레 방향으로 차지하는 크기가 위치마다 다르다
- 12시·6시에서는 폭, 3시·9시에서는 높이가 둘레 방향을 차지한다
- 위치별로 나누지 않고 큰 쪽 `max(폭, 높이)` 하나로 판정한다

### 모닥불

| 계산용 너비 | 높이 |
|---|---|
| 360 이상 | 9 도트 |
| 360 미만 | 7 도트 |

원본은 `design/prototype.html`의 `S.fireCold`, `S.fireA`, `S.fireB`다.

```
9 도트 (기본)
붙지 않음    타는 중 A    타는 중 B
.......      ...y...      ..y....
.......      ..yoy..      .yoyo..
.......      .yoooy.      .yoooy.
.......      .roooor      rooooor
.......      .rroorr      .rroorr
.......      ..rrr..      ..rrr..
..DDD..      ..DDD..      ..DDD..
.DWWWD.      .DWWWD.      .DWWWD.
DWWWWWD      DWWWWWD      DWWWWWD

7 도트 (좁은 화면. 위 두 줄 제거)
붙지 않음    타는 중 A    타는 중 B
.......      .yoooy.      .yoooy.
.......      .roooor      rooooor
.......      .rroorr      .rroorr
.......      ..rrr..      ..rrr..
..DDD..      ..DDD..      ..DDD..
.DWWWD.      .DWWWD.      .DWWWD.
DWWWWWD      DWWWWWD      DWWWWWD
```

| 글자 | 색 역할 (§2) |
|---|---|
| `y` | 불 심 `#fff2a8` |
| `o` | 불 가운데 `#ffb43c` |
| `r` | 불 아래 `#e04a2b` |
| `W` | 장작 `#8a6238` |
| `D` | 장작 그늘 `#4a3220` |
| `.` | 비움 |

### 스파크 손잡이

반짝임은 A와 B를 번갈아 그린다. 정지는 A에 고정한다. 중앙을 비우지 않는다. 비우면 호 끝이 그 사이로 비친다.

원본은 `design/prototype.html`의 `S.sparkA`, `S.sparkB`, `S.sparkRest`다.

```
반짝임 A    반짝임 B    휴식 설정 중
...h...     .......     ...k...
...h...     ...h...     ...k...
..hHh..     ..hHh..     ..kKk..
hHHHHHh     .hHHHh.     kKKKKKk
..hHh..     ..hHh..     ..kKk..
...h...     ...h...     ...k...
...h...     .......     ...k...
```

| 글자 | 색 역할 (§2) |
|---|---|
| `h` | 집중 손잡이 갈래 `#c9bb98` |
| `H` | 집중 손잡이 심 `#fff8e0` |
| `k` | 휴식 손잡이 갈래 `#2f7b85` |
| `K` | 휴식 손잡이 심 `#9fe3ea` |
| `.` | 비움 |

### 조작 버튼

| 항목 | 값 |
|---|---|
| 크기 | 28 × 28 도트 (56 × 56 논리 픽셀) |
| 아이콘 | 12 × 12 도트, 중앙 정렬 |
| 터치 영역 | 64 × 64 논리 픽셀 |
| 테두리 | 1 도트 |
| 하이라이트 | 위·왼쪽 안쪽 1 도트 |
| 그림자 | 아래·오른쪽 안쪽 1 도트 |
| 리벳 | 네 모서리 안쪽 3 도트 자리에 1 도트씩 |
| 눌림 | 1 도트 내려앉고 하이라이트 제거, 90밀리초 |

원본은 `design/prototype.html`의 `ICONS[12]`다.

```
재생            일시정지        정지
...I........    ..III..III..    ............
...II.......    ..III..III..    ............
...III......    ..III..III..    ..IIIIIIII..
...IIII.....    ..III..III..    ..IIIIIIII..
...IIIII....    ..III..III..    ..IIIIIIII..
...IIIIII...    ..III..III..    ..IIIIIIII..
...IIIIII...    ..III..III..    ..IIIIIIII..
...IIIII....    ..III..III..    ..IIIIIIII..
...IIII.....    ..III..III..    ..IIIIIIII..
...III......    ..III..III..    ..IIIIIIII..
...II.......    ..III..III..    ............
...I........    ..III..III..    ............
```

| 글자 | 색 역할 (§2) |
|---|---|
| `I` | 아이콘 `#f4ead6`. 잠긴 버튼은 `#6f6688` |
| `.` | 비움 |

## 5. Layout

### 시계판 구조

시계판은 60분 한 바퀴다. 12시가 0분이고 시계 방향으로 는다. 화면 크기와 설정 시간에 관계없이 장작 48개와 모닥불 12개, 60칸을 항상 다 그린다. 설정 밖 칸은 타지 않은 상태로 어둡게 깔린다.

### 좌표 규칙

칸 `i`(1~60)는 `i-1`분에서 `i`분까지다.

- 개체 중심 각도는 `(i - 0.5) × 6`도
- 모닥불은 5분 배수 칸의 중심에 둔다
- 모닥불 칸에는 장작을 두지 않는다
- 눈금 숫자는 0부터 55까지 5분 간격 12개. 값 `v`를 `v × 6`도, 칸 경계에 둔다
- 손잡이 각도는 남은 분 `r`의 `r × 6`도다. 시간이 가면 반시계로 물러난다
- 호는 0도부터 손잡이까지다
- 모든 개체는 회전하지 않는다

### 배치 순서

바깥에서 안쪽 순서다.

| 요소 | 배치 |
|---|---|
| 자동 시작 카운트다운 | 0분 눈금 바깥, 눈금과 12 논리 픽셀 여백 |
| 눈금 숫자 | 반지름 175 |
| 모닥불·장작 | 반지름 153 |
| 기준 표식 | 12시, 개체와 같은 반지름 |
| 손잡이 | 반지름 131 |
| 호 | 12시부터 손잡이까지, 반지름 131 |
| 집중 타이머 숫자 | 시계판 중심. 아래 숫자가 생겨도 위로 밀지 않는다 |
| 휴식 타이머 숫자 | 집중 타이머 숫자 아래 10 도트 |
| 조작 버튼 두 개 | 시계판 밖, 화면 아래. 세로 위치는 §7 계산 순서를 따른다. 390×844 화면에서 중심 y 660, x 151과 239 (간격 88) |

### 기준 화면 치수

너비 390, 높이 844, safe area 위 47·아래 34, 배율 1, 모닥불 9 도트 기준이다. 시계판 중심 y는 360이다.

| 항목 | 논리 픽셀 | 도트 |
|---|---|---|
| 시계판 지름 | 306 | 153 |
| 시계판 원주 | 961 | 481 |
| 1분 간격 | 16.02 | 8.01 |
| 개체 중심 반지름 | 153 | 76.5 |
| 눈금 숫자 반지름 | 175 | 87.5 |
| 호·손잡이 반지름 | 131 | 65.5 |
| 호 두께 | 6 | 3 |

### 겹침 검산

1분 간격 최악값은 7.51 도트다. 원주 방향이다.

| 쌍 | 필요 | 여유 |
|---|---|---|
| 장작 + 장작 | 4.0 | 3.51 |
| 모닥불 + 장작 | 6.5 | 1.01 |
| 모닥불 + 모닥불 | 9.0 | 30.6 |

반지름 방향이다.

| 쌍 | 간격 (논리 픽셀) |
|---|---|
| 호(131) ↔ 빛 번짐 최대(145, 25칸 점등) | 14 |
| 스파크 바깥(138) ↔ 개체 안쪽(144) | 6 |
| 개체 바깥(162) ↔ 눈금 숫자 안쪽(168) | 6 |
| 눈금 숫자 바깥(중심 y + 182 = 542) ↔ 버튼 위(632) | 90 |
| 버튼 아래(688) ↔ safe area 아래 끝(810) | 122 |

버튼 터치 영역은 119~183과 207~271로 24 떨어진다.

### 개체 중심은 격자 정렬의 예외

원 위 60등분은 정수 도트로 떨어지지 않는다. 중심 위치만 실수로 계산하고 그릴 때 반올림한다. 스프라이트 내부는 격자에 정렬한다.

## 6. Depth

레이어가 둘이다. 아래에서 위로 쌓는다.

| 순서 | 레이어 | 무엇이 사는가 | 안티에일리어싱 | 좌표계 |
|---|---|---|---|---|
| 1 | 부드러운 레이어 | 호, 빛 번짐, 불티 잔광 | 켬 | 논리 픽셀 |
| 2 | 도트 레이어 | 장작, 모닥불, 손잡이, 기준 표식, 불꽃, 불티, 숫자, 조작 버튼 | **끔** | 도트 |

빛 번짐은 장작 반지름 16, 모닥불 반지름 32 논리 픽셀로 번진다. 반지름별 텍스처 두 장을 앱 시작 때 한 번 만들고, 매 프레임에는 알파와 스케일만 바꿔 그린다. 매 프레임 블러를 계산하지 않는다.

## 7. Responsive Behavior

브레이크포인트가 없다. **정수 배율만 쓴다.**

### 확정값 네 개

| 값 | 확정 |
|---|---|
| 기준 너비 | 375 논리 픽셀 |
| 시계판 기준 지름 | 306 논리 픽셀 |
| 모닥불 9 도트 전환 너비 | 360 논리 픽셀 |
| 도트 크기 (배율 1) | 2 논리 픽셀 |

### 계산 순서

```text
1. 기준 너비        = 375 (확정값)
2. 배율 k           = max(1, 내림(짧은 변 ÷ 375))
3. 계산용 너비       = 짧은 변 ÷ k
4. 모닥불 높이       = 계산용 너비 < 360 이면 7 도트, 아니면 9 도트
5. 개체 중심 반지름   = min(계산용 너비 ÷ 2 − 8 − 20 − 모닥불 반높이, 153)
6. 눈금 숫자 반지름   = 개체 중심 반지름 + 모닥불 반높이 + 6 + 7
7. 호·손잡이 반지름   = 개체 중심 반지름 − 모닥불 반높이 − 13
8. 1분 간격(도트)    = π × 개체 중심 반지름 ÷ 60
9. 버튼 중심 y       = safe area 아래 끝 − 150
10. 시계판 중심 y     = 버튼 중심 y − 300
11. 화면에 그릴 때 모든 값에 k를 곱한다. 도트 크기도 2k가 된다
```

- 상수로 두는 값은 확정값 네 개다. 나머지는 계산 순서로 구한다
- 시계판 위 끝(중심 y − 182)이 safe area 위 끝보다 위로 가지 않으려면 safe area 높이가 632 이상이어야 한다

### 구간별 처리

| 짧은 변 | 배율 | 모닥불 |
|---|---|---|
| 338~359 | 1배 | 7 도트 |
| 360~ | 1배 | 9 도트 |

지원 최소 너비는 338 논리 픽셀이다.

## 8. States

상태는 모드(집중·휴식)와 단계(대기·진행·일시정지·완료)의 조합이다.

### 층별 동작

| 층 | 대기 | 진행 | 일시정지 | 완료 |
|---|---|---|---|---|
| 불꽃, 불티, 스파크 | 돈다 | 돈다 | **멈춤** | 돈다 |
| 호, 손잡이, 숫자 | 멈춤 | 움직임 | 멈춤 | 멈춤 |
| 차오름 | 없음 | 있음 | 멈춤 | 없음 |
| 불 밝기 | 100% | 100% | **35%** | 100% |

### 손잡이 색과 움직임

| 상황 | 색 | 움직임 |
|---|---|---|
| 집중 타이머 대기, 진행 | 크림 | 반짝임 |
| 휴식 타이머를 설정하는 중 | 청록 | 정지 |
| 휴식 타이머 진행 | 크림 | 정지 |

### 개체 상태

| 상태 | 개수 |
|---|---|
| 타고 있는 모닥불 | 0개 이상 |
| 불붙은 장작 | 0개 이상 |
| 차오르는 중인 개체 | 진행에서 1개, 일시정지에서 1개(멈춘 채), 그 외 0개 |
| 아직 붙지 않은 개체 | 나머지 전부 |

### 점화 판정

| 조건 | 상태 |
|---|---|
| `i - 1 ≥ r` | 완전히 붙었다 |
| `i > r > i - 1` | 차오르는 중. 진행률 `i - r` |
| `i ≤ r` | 아직 붙지 않았다 |

칸 `i`는 경과 `설정 − i + 1`분에 완전히 붙는다.

### 조작

| 입력 | 동작 |
|---|---|
| 손잡이 끌기 | 고른 대상의 시간 설정. 대기 상태에서만 |
| 가운데 숫자 탭 | 집중 타이머를 편집 대상으로 |
| 아래 숫자 탭 | 휴식 타이머를 편집 대상으로 |
| 재생·일시정지 버튼 | 시작, 일시정지, 재개, 다음 단계로 |
| 정지 버튼 | 중단하고 집중 타이머 대기로 |

| 고른 대상 | 호 색 | 범위 |
|---|---|---|
| 집중 타이머 (가운데) | 보라 | 1~60분 |
| 휴식 타이머 (아래) | 청록 | 0~60분 |

각 타이머는 대기 상태에서 설정할 수 있다.

### 휴식 타이머

손잡이가 시계 방향으로 돌아가며 불이 꺼지고 호가 차오른다. 호는 보라색이다.

- 집중 타이머가 끝나고 5초 뒤 자동으로 시작된다
- 휴식 타이머가 끝나면 집중 타이머 대기로 간다
- 휴식 타이머가 `00:00`이면 재생 버튼을 눌렀을 때 집중 타이머가 시작된다

## 9. Motion & Easing

| 항목 | 단위 |
|---|---|
| 위치 | 도트 |
| 속도 | 도트/프레임 (프레임 = 16밀리초). 실제 프레임 간격 `dt`에는 `dt ÷ 16`을 곱한다 |
| 이징 | 표준 이징 함수 이름 |

### 완료

| 항목 | 값 |
|---|---|
| 연출 길이 | 3500밀리초 |
| 번짐 반경 배율 | 모닥불 최대 2.5배, 장작은 증가분의 절반이라 최대 1.75배 |
| 타오름 감쇠 | 1200밀리초. 경과 비율이 `t`일 때 `배율 = 1 + 1.5 × (1 − t)²` |
| 불티 개수 | 80개 |
| 불티 등장 지연 | 200밀리초 |
| 불티 수명 | 2.8초 (0.75~1.25배 편차) |
| 불티 상승 속도 | 0.60 도트/프레임 (0.55~1.45배 편차) |
| 불티 좌우 퍼짐 | ±0.22 도트/프레임. 여기에 시계판 바깥 방향으로 0.10 도트/프레임을 더한다 |
| 불티 감쇠 | 프레임마다 세로 속도에 0.994, 가로 속도에 0.985를 곱한다 |
| 불티 색 | 수명 0.66 위는 불 심, 0.33 위는 불 가운데, 아래는 불 아래 |
| 불티 잔광 | 반지름 3 논리 픽셀, 알파 `0.15 × 수명` |

### 일시정지

| 항목 | 값 |
|---|---|
| 잦아드는 시간 | 500밀리초, `easeInOutQuad` |
| 재개 | 같은 시간에 원래대로 |
